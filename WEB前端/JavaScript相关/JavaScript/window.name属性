window.name是浏览器开放出来的一个接口，设置该属性的值后，及时页面发生了跳转，这个值依然不会变化，并且可以跨域使用。
虽然看起来十分便捷，但是同样也存在一个问题，就是window.name属性值可以直接操作的。
为了避免以上问题，我们可以使用这样一个模块，让所有关于window.name的操作都通过这个模块的接口来使用。
这里推荐一款别人已经实现的nameStorage模块(aralejs:https://github.com/aralejs/name-storage)，详情可以自行观看。
PS:实现原理本质上也不难，当然你自己也可以实现这样一个模块。
